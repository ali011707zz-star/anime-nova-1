<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-feature
    android:name="android.software.picture_in_picture"
    android:required="false" />
  <application>
    <!--
      IsolatedPlayerActivity/IsolatedPlayerModule are documented in
      artifacts/nova-mobile/docs/NOVA_PLAYER_FULL_ISOLATION_PLAN.md as
      "implemented" but the Kotlin sources were never actually committed to
      the repo (confirmed via git history — they never existed). Referencing
      them here broke the Android build (manifest/ExpoModulesPackageList
      pointed at classes that don't exist). Removed until the isolated-player
      feature is actually implemented; see the proposed follow-up task.
    -->
  </application>
</manifest>