---
name: Session checkpoint 2026-07-13 (AniTaku patch abandoned, Videasy frontend fix, Xpass dead links)
description: Where a multi-part session left off — AniTaku anti-tamper patch stopped, Videasy/VidKing frontend wiring fixed, Xpass source diagnosed as dead-on-arrival, two more embed sources requested next.
---

- **AniTaku APK patch — abandoned by user request.** Root cause chain fully diagnosed (in order found): (1) missing zipalign/v2-v3 signing → fixed; (2) app has a native/Java installed-package scan (`Lu8/m;` in classes.dex) that hard-blocks launch if any of ~20 known sniffer/proxy packages are installed, including PCAPdroid (`com.emanuelef.remote_capture`) — smali-patched to always report "not detected"; (3) after that dex edit the app silently crashes on open (no logcat access to confirm), most likely a **separate native integrity/tamper check** (suspect `libapminsighta.so`/`libapminsightb.so`) that detects the modified classes.dex and self-destructs — never confirmed since the user has no way to capture logcat. A Frida-gadget injection (via `objection patchapk`) was attempted as a stealthier alternative (hook at runtime instead of patching bytecode) but objection's bundled apktool put the gadget `.so` in a folder literally named `arm64` instead of the required `arm64-v8a`, so Android would silently ignore it — fix identified but not applied. **User decided to stop here — too complex without a crash-log feedback loop.** If resumed: get logcat access first (even a non-root "Catlog"/"Logcat Reader" app), don't guess-and-check blind again.
- **Videasy/VidKing anime source found missing from UI — root cause: not a scraper bug.** Backend (`getVideasyAnimeSources` / `videasy_anim` case) was fully working (per `videasy-wingsdatabase-decrypt.md`), but the anime section's frontend `SCRAPER_DEFS` array in `artifacts/anime-scraper/src/pages/Watch.tsx` never had a `videasy_anim` entry — so the UI simply never requested/showed it. Movies/TV section (`AnimationWatch.tsx`) doesn't need this (it auto-fetches all backend-registered scrapers via `/api/animation/sources-stream`, no frontend allowlist). **Lesson: when a working backend scraper "doesn't show up" in the anime (not movies) UI, check `Watch.tsx`'s `SCRAPER_DEFS` array first — that's a separate manual allowlist from the backend switch/case list.** Fixed by adding the missing entry (tag "VE", audioLang "en").
- **Xpass ("XP" tag) source — API works, CDN links are dead on arrival.** `play.xpass.top/meg/{movie|tv}/...../playlist.json` reliably returns 200 + valid JSON with an `enproxy` HLS URL on `ps1.1x2.space`, but that URL itself 404s immediately on first fetch (tested across 3 different titles, both movie and tv, zero delay) — not a timing/expiry race, the links appear to be stale/broken at the source. This matches the user's "appears sometimes but doesn't work" symptom. Not yet fixed; likely needs a different endpoint/param from xpass or the site's own multi-CDN fallback (VIP slot) investigated — only MEG slot was tested.
- **Next requested step (not started):** continue reverse-engineering 2 more embed sources (طلب المستخدم: "استمر بعمل هندسة عكسية لمصدرين تضمين وكفي" — user wants exactly 2 more, then stop). No candidates chosen yet for this session's continuation — check `embed-decrypt-research-2026-07-12.md` for the untested-provider queue before picking new ones from scratch.
- mediaflow-proxy-light (Rust binary + pm2 process on the VPS, port 8888) was removed per user request — it was installed but never wired into the app code (only manually curl-tested, last okru extraction test failed with "OKVideo div not found").
